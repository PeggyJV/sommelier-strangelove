diff --git a/node_modules/@chakra-ui/react/dist/cjs/button/use-button-type.cjs b/node_modules/@chakra-ui/react/dist/cjs/button/use-button-type.cjs
index 5868de6..90befab 100644
--- a/node_modules/@chakra-ui/react/dist/cjs/button/use-button-type.cjs
+++ b/node_modules/@chakra-ui/react/dist/cjs/button/use-button-type.cjs
@@ -4,13 +4,14 @@
 var React = require('react');
 
 function useButtonType(value) {
-  const [isButton, setIsButton] = React.useState(!value);
+  // React 19 fix: avoid setState inside ref callbacks to prevent infinite
+  // update loops during commitAttachRef.  Track the value in a ref instead.
+  const isButtonRef = React.useRef(!value);
   const refCallback = React.useCallback((node) => {
-    if (!node)
-      return;
-    setIsButton(node.tagName === "BUTTON");
+    if (!node) return;
+    isButtonRef.current = node.tagName === "BUTTON";
   }, []);
-  const type = isButton ? "button" : void 0;
+  const type = isButtonRef.current ? "button" : void 0;
   return { ref: refCallback, type };
 }
 
diff --git a/node_modules/@chakra-ui/react/dist/esm/button/use-button-type.mjs b/node_modules/@chakra-ui/react/dist/esm/button/use-button-type.mjs
index 88188c4..f33a869 100644
--- a/node_modules/@chakra-ui/react/dist/esm/button/use-button-type.mjs
+++ b/node_modules/@chakra-ui/react/dist/esm/button/use-button-type.mjs
@@ -1,14 +1,15 @@
 'use client';
-import { useState, useCallback } from 'react';
+import { useRef, useCallback } from 'react';
 
 function useButtonType(value) {
-  const [isButton, setIsButton] = useState(!value);
+  // React 19 fix: avoid setState inside ref callbacks to prevent infinite
+  // update loops during commitAttachRef.  Track the value in a ref instead.
+  const isButtonRef = useRef(!value);
   const refCallback = useCallback((node) => {
-    if (!node)
-      return;
-    setIsButton(node.tagName === "BUTTON");
+    if (!node) return;
+    isButtonRef.current = node.tagName === "BUTTON";
   }, []);
-  const type = isButton ? "button" : void 0;
+  const type = isButtonRef.current ? "button" : void 0;
   return { ref: refCallback, type };
 }
 
