query GetWeeklyTVL {
  cellarDayDatas(
    first: 7
    orderDirection: asc
    orderBy: date
    where: { tvlActive_gt: 0 }
  ) {
    date
    asset {
      symbol
      decimals
    }
    tvlTotal
  }
}
