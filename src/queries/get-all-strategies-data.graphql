query GetAllStrategiesData {
  cellars {
    id
    dayDatas(orderBy: date, orderDirection: desc) {
      date
      shareValue
    }
    tvlTotal
    asset {
      id
      symbol
      decimals
    }
    positions
    shareValue
  }
}
