query GetHourlyTVL($epoch: Int) {
  cellarHourDatas(
    orderDirection: asc
    orderBy: date
    where: { date_gte: $epoch, tvlActive_gt: 0 }
  ) {
    date
    asset {
      symbol
      decimals
    }
    tvlTotal
  }
}
