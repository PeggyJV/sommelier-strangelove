query GetHourlyShareValue($epoch: Int!, $cellarAddress: String) {
  cellarHourDatas(
    orderDirection: asc
    orderBy: date
    where: { cellar: $cellarAddress, date_gte: $epoch }
  ) {
    date
    shareValue
  }
}
