query Get10DaysShareValue($epoch: Int!, $cellarAddress: ID!) {
  cellar(id: $cellarAddress) {
    dayDatas(
      first: 10
      orderDirection: asc
      orderBy: date
      where: { date_gte: $epoch }
    ) {
      date
      shareValue
    }
  }
}
