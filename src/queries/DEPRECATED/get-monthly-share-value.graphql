query GetMonthlyShareValue($epoch: Int!, $cellarAddress: ID!) {
  cellar(id: $cellarAddress) {
    dayDatas(
      first: 31
      orderDirection: asc
      orderBy: date
      where: { date_gte: $epoch }
    ) {
      date
      shareValue
    }
  }
}
